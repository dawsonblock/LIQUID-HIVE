[pytest]
pythonpath = .
addopts = -q
# Only run tests in top-level tests directory to avoid duplicate copies in nested zip folders
testpaths = tests
norecursedirs = LIQUID-HIVE-main .git .emergent __pycache__ node_modules